let express,connectDatabase,cors,tasksRoute;_a05‍.w("express",[["default",["express"],function(v){express=v}]]);_a05‍.w("./config/db",[["default",["connectDatabase"],function(v){connectDatabase=v}]]);_a05‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_a05‍.w("./routes/tasks",[["default",["tasksRoute"],function(v){tasksRoute=v}]]);

//import { check, validationResult } from 'express-validator';



// Initialize express
const app = express();

// Connect to database
connectDatabase();

// Middlewares
app.use(express.json({ extended: false })); //handles JSON files
app.use(
    cors({
        origin: 'http://localhost:3000'
    })
);

// Set up a static folder for front-end
app.use(express.static('public'));


// Routes
app.use('/api/tasks', tasksRoute);


// API endpoints
app.get('/', (req, res) => 
    res.send('http get request sent to root api end point.')
);

// Connection listener
app.listen(3000, () => console.log(`Express server running on port 3000`));