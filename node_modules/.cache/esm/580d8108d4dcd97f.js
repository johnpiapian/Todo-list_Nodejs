let mongoose,config;_d4d‍.x([["default",()=>_d4d‍.o]]);_d4d‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_d4d‍.w("config",[["default",["config"],function(v){config=v}]]);


// Get the connection string
const db = config.get('mongoURI');

// Connect to MoongoDB
const connectionDatabase = async () => {
  try {
    await mongoose.connect(db, {
      useUnifiedTopology: true
    });
    console.log('Connected to MongoDB');
  } catch (error) {
    _d4d‍.g.console.error(error.message);

    // Exit with failure code
    process.exit(1);
  }
};


_d4d‍.d(connectionDatabase);